#include <bits/stdc++.h> 
int pairSum(vector<int> &arr, int n, int target){
	int j=0;
	int z=0;
	for(int i=0;i<n;i++){
		for(int j=i+1;j<n;j++){
			if(arr[i]+arr[j]==target){z++;}
		}
	}
	if(z!=-1){return z;}
	else{return -1;}
}
